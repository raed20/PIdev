{% extends 'base.html.twig' %}

{% block body %}
<!doctype html>

<body>
<h1>List Bank</h1>
<table border="1">
<tr>
    <th>Id</th>
    <th>Nom</th>
    <th>email</th>
    <th>phone</th>
    <th>amount</th>
    <th>duration</th>

   
 
</tr>

      <tr>  {% for a in pret %} </tr>
        <td>{{ a.id }}</td>
        <td>{{ a.fname }}</td>
        <td> {{ a.email }}</td>
        <td> {{ a.phonenum }}</td>
        <td> {{ a.amount }}</td>
        <td> {{ a.duration }}</td>


        <td><a href="{{ path('app_editloan', {'id': a.id}) }}">edit</a></td>
        <td><a href="{{ path('app_deleteloan', {'id': a.id}) }}">delete</a></td>


    {% endfor %}

</table>
</body>
</html>
{% endblock %}
