
{% block body %}
{{ form_start(formadd, {'attr': {'class': 'my-4'}}) }}
    <div class="form-group mb-4">
        {{ form_label(formadd.contenue, 'Message', {'label_attr': {'class': 'font-weight-bold'}}) }}
        {{ form_widget(formadd.contenue, {'attr': {'class': 'form-control', 'rows': 8, 'placeholder': 'Enter your message'}}) }}
    </div>
    <div class="form-group">
        <button type="submit" class="btn btn-primary btn-block rounded-pill py-3">Confirm</button>
    </div>
{{ form_end(formadd) }}

{% endblock %}