<!doctype html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport"
          content="width=device-width, user-scalable=no, initial-scale=1.0, maximum-scale=1.0, minimum-scale=1.0">
    <meta http-equiv="X-UA-Compatible" content="ie=edge">
    <title>ProSync</title>
</head>
<body>
<h1>List Bank</h1>
<table border="1">
<tr>
    <th>Id</th>
    <th>Nom</th>
    <th>Adresse</th>
   
 
</tr>

      <tr>  {% for a in Bank %} </tr>
        <td>{{ a.id }}</td>
        <td>{{ a.Nom }}</td>
        <td> {{ a.Adresse }}</td>
        <td><a href="{{ path('app_editbank', {'id': a.id}) }}">edit</a></td>
        <td><a href="{{ path('app_deletebank', {'id': a.id}) }}">delete</a></td>


    {% endfor %}

</table>
</body>
</html>